# Project Objective: H3632 — commit-guard hardening (audit gap G2)

## ➡️ Next Steps (Queue)

- [ ] None — H3632 closed (PR #2894 merged 29-08-2026). Nearest-root audit sampling (below) remains parked with a human reviewer.

## 🚧 Current Work-In-Progress (WIP)

- [ ] Manual audit of `v02/etymology_stats/nearest_root_audit_sample.csv` remains for a human reviewer (pre-existing objective, untouched by H3632).

## 🧠 Dev Notes & Hypotheses

- 2026-08-29: H3632 (audit G2, findings O3–O6/O10) landed as tooling-only PR #2894 — no `v02/` source touched. Hook now validates staged blobs (`git show :v02/...`), refuses staged-vs-worktree drift in the generate stage, fails zero-`<LEND>` sources, and blocks unacked canonical D/R via `scripts/check_deletions.sh`. Install is `git config core.hooksPath .githooks` (relative; survives clone moves); hook lives in `.githooks/`. CI backstop `guard-backstop.yml` replicates both stages on hook-less clones. Scenario suite: `bash scripts/test_pre_commit_guard.sh` (all pass). Note for correction sessions: an unacked `git rm` of a canonical `<dict>.txt` now fails the commit — ack with `CSL_ORIG_ACK_DELETION="v02/<d>/<d>.txt"` only when intentional.

## ✅ Completed (Recent only)

- 2026-08-29 (OxAlpha `opencode/z-ai/glm-5.3-flash`): H3632 shipped — [PR #2894](https://github.com/sanskrit-lexicon/csl-orig/pull/2894) merged green (encoding + backstop ×2). All five audit findings re-verified at `dee5a03` before fixing.
- 2026-07-04 (Sonnet 5 `claude-sonnet-5`): ACL-Anthology adoption pass on the etymology paper (venue framing verified; submission-readiness note added to `PAPER_DRAFT.md`).
- 2026-06-26: STC issue #2821 applied via `updateByLine.py` (BOM-checked, XML-validated); etymology audit sampler added.
